
#include <string>
#include <vector>
#include "zmBase/structurs.h"

using namespace std;

extern vector<ZM_Base::worker> _workers;
extern vector<ZM_Base::manager> _managers;
extern ZM_Base::scheduler _schedr;

void receiveHandler(const string& cp, const string& data){


}